# Template: React Native / Expo (Step 6)

## Role

**Primary:** Mobile

## Goal

**Expo SDK 52 + Expo Router + NativeWind + Apollo + Clerk (expo) + Redux-Persist** per `.claude/standards/mobile.md` when present.

## Aliases

`--react-native` and `--expo` load this template.

## Constraints

- `SafeAreaView` on screen roots; `FlashList` for lists; `expo-image`; 44pt touch targets.
- Platform gates for maps and push as applicable.

## Acceptance

- [ ] EAS-ready `app.json` / `eas.json` documented
- [ ] Deep link scheme for Clerk OAuth
