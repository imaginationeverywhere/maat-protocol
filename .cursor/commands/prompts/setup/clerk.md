# Template: Clerk auth (Step 4)

## Role

**Primary:** Frontend + Backend · **Secondary:** Security

## Goal

Implement **Clerk** end-to-end: middleware, hooks-based sign-in/up, SSO callback route, backend JWT verification, webhook sync — per **`.claude/standards/clerk-auth.md`** (mandatory when this flag is used).

## Execution note

The executing agent MUST load and follow `.claude/standards/clerk-auth.md` in full; this template only scopes the **Heru deliverables** checklist.

## Acceptance

- [ ] ProfileWidget on authenticated layouts (web)
- [ ] `docs/standards/clerk.md` created/updated in Heru
