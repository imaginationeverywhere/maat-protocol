# Template: Deploy — GCP (Step 8 alt)

## Role

**Primary:** DevOps

## Goal

When **GCP** is the primary cloud: Cloud Run / GKE / Artifact Registry as appropriate; IAM least-privilege; secrets in Secret Manager.

## Constraints

- Parity with AWS baseline docs where concepts map (staging/prod, no keys in git).

## Acceptance

- [ ] Build and deploy pipeline documented
- [ ] Custom domains and TLS approach listed
