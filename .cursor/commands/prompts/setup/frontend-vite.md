# Template: Frontend — Vite (Step 2 alt)

## Role

**Primary:** Frontend

## Goal

When **Next.js is not** the shell, use **Vite + React + TypeScript** with the same **Apollo + Clerk + Redux** stack where applicable, aligned to project conventions.

## Constraints

- SPA routing vs SSR: document chosen pattern.
- Keep API base URL and auth callback URLs consistent with backend and Clerk dashboard.

## Acceptance

- [ ] Dev server and production build succeed
- [ ] Env template checked in (`.env.example`)
